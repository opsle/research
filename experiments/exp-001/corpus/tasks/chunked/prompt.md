Implement `chunked(items, size)` in `task.py`.

Return a new list of lists containing consecutive chunks of `items`. Preserve
order and include a final short chunk. `size` must be a positive integer and a
boolean is not an integer for this contract; otherwise raise `ValueError`. Do
not mutate the input list.
