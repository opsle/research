Implement `slugify(value)` in `task.py`.

The input is a string. Return lowercase ASCII letter and digit runs joined by a
single hyphen. Any other character is a separator. The result has no leading or
trailing hyphen. Return the empty string when no ASCII letter or digit remains.
Raise `TypeError` when `value` is not a string.
