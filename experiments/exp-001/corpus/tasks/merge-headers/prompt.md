Implement `merge_headers(pairs)` in `task.py`.

Each pair contains a non-empty string name and a string value; invalid pairs
raise `ValueError`. Header names are case-insensitive. Preserve the first
spelling and first position of each name while the last value wins. Return a new
list of two-element lists and do not mutate the input.
