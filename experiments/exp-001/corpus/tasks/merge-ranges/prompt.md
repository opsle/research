Implement `merge_ranges(ranges)` in `task.py`.

Each item must be a two-element list of integers `[start, end]` with
`start <= end`; otherwise raise `ValueError`. Return new sorted lists combining
closed ranges that overlap or touch. Do not mutate the input.
