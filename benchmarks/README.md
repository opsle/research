# Benchmarks

Cross-project plans include semantic-edit → trajectory profiling; context-firewall → decision evidence → supervisor; event wakeup → supervisor; verifiable handoff → fresh review; and routing → quality + price. No numbers are claimed before published fixtures, correctness gates, repetitions, and observable evidence.

EXP-001's provider-free benchmark components are frozen at
[`experiments/exp-001/`](../experiments/exp-001/). The corpus, oracle, arm
contracts, and offline interoperability harness are published, but the exact
model/provider configuration, repetition count, committed allocation seed and
mapping, and subject adapter remain unset. EXP-001 therefore remains `PLANNED`
and no lifecycle promotion or comparative claim follows from the offline
qualification.
